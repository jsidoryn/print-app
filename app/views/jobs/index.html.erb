<h1>List all jobs</h1>

<% @jobs.each do |job| %>
  <%= link_to job.title, job_specifications_path(job) %>
  <%= link_to "Delete", job_path(job), method: :delete, data: { confirm: "Are you sure" } %><br>
<% end %>

<p>
  <%= link_to "New job", new_job_path %>
</p>
